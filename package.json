{
  "name": "medicine-prescription-analyzer",
  "version": "1.0.0",
  "description": "A system to analyze medicine prescriptions using AWS services",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "aws s3 sync dist/ s3://$S3_BUCKET_NAME --delete"
  },
  "dependencies": {
    "aws-sdk": "^2.1490.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "terser": "^5.24.0"
  }
}